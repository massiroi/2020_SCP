-- generated by cds-compiler version 1.35.0
VIEW HRservice_EmployeeSrv AS SELECT
  Employee_0.ID,
  Employee_0.NAME,
  Employee_0.SALARY,
  Employee_0.Dept_ID
FROM my_Employee AS Employee_0
WITH ASSOCIATIONS (
  MANY TO ONE JOIN HRservice_DeptSrv AS Dept ON (Dept.ID = Dept_ID)
)